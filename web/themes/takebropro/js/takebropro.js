/**
 * @file
 * takebropro behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.takebropro = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
